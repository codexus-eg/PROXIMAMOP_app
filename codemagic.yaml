workflows:
  proximamop-ios-testflight-workflow:
    name: PROXIMAMOP iOS Build and Publish to TestFlight
    max_build_duration: 120
    instance_type: mac_mini_m2

    integrations:
      app_store_connect: noourway

    environment:
      xcode: latest
      ios_signing:
        provisioning_profiles:
          - PROXIMAMOP_Manual_Profile
        certificates:
          - ios_cert_manual

    scripts:
      - name: Install .NET 10 SDK and MAUI Workload
        script: |
          curl -L https://dot.net/v1/dotnet-install.sh -o dotnet-install.sh
          chmod +x ./dotnet-install.sh
          ./dotnet-install.sh --channel 10.0

          # تعريف المسار للخطوة الحالية
          export PATH=$HOME/.dotnet:$PATH
          dotnet workload install maui

      - name: Apply Vault Profiles
        script: xcode-project use-profiles

      - name: Build IPA
        script: |
          # السطر السحري لحل المشكلة: تذكير السيرفر بمكان dotnet قبل البناء
          export PATH=$HOME/.dotnet:$PATH

          dotnet publish PROXIMAMOP.csproj \
            -f net10.0-ios \
            -c Release \
            -p:ArchiveOnBuild=true \
            -p:RuntimeIdentifier=ios-arm64

    artifacts:
      - "**/*.ipa"

    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
      email:
        recipients:
          - codexus.eg@gmail.com
